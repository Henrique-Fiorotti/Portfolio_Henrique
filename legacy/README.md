# Versão anterior

Arquivos da versão HTML/CSS/JavaScript/PHP original, mantidos como referência.
O site atual usa `app/`, `components/` e `data/`. Esta pasta não é publicada pelo
Next.js e não deve ser usada como diretório de saída da hospedagem.
